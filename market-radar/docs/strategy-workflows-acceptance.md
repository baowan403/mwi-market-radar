# Personalized Strategy Workflows Acceptance

日期：2026-09-01（Asia/Taipei）

## Fresh automated evidence

- Unit：44 files、433 passed、0 failed。
- Build：TypeScript、dashboard、userscript 全部 exit 0；`dist/strategy-data.json` 存在且只在策略頁使用。
- Chrome E2E：34 passed、2 個 desktop-only expected skips、0 failed。

## Covered behavior

- Milkonomy Exporter v1／preset 角色匯入與多角色隔離。
- 真實 MWI 製造、裁縫、鍛造、烹飪與沖泡配方。
- 2–7 步工作流工時平衡、中間品抵消、循環阻擋。
- 海盜精煉碎片分解、海盜精華點金及組合路徑。
- 無催化、專用催化、至高催化成功率與成本。
- 角色裝備、房屋、茶、社群、成就、封印、神龕。
- 理論日利、每小時利潤、24h 資金與步驟明細。
- 策略釘選跨 reload 保存。
- 角色名稱、character id、裝備 HRID 不出現在 request body。

## Player-facing limitation

本階段只發布理論收益，UI 明確顯示「尚未套用市場承接量」。流動性調整、安全批量、售完天數、3D／7D 策略趨勢與回測仍是後續必要工作，不能以本文件宣稱完整目標已完成。

## 2026-09-02 角色假設與數字格式驗收

1. 在 Milkonomy 設定 `jotaro99` 的各生活技能裝備、三種茶與房屋後，匯出並導入 Radar。
2. 重開「角色快照」，在「目前計算配置」展開煉金，逐項核對工具、上下衣、口袋、三茶與實驗室等級。
3. 在「共用增益」核對社群、生產效率、神龕與成就。沒有匯入的欄位必須顯示「未設定」，不得從 `inventoryMap` 自動替換。
4. 進入行情表、物品詳情與策略推薦，確認 `1,234` 顯示為 `1.23K`、`56,430,000` 顯示為 `56.43M`、`1,000,000,000` 顯示為 `1,000M`。
5. 全站不得出現 `B` 數字後綴；K／M 僅影響顯示，排序與收益計算仍使用未取整原值。

## 2026-09-02 策略表排版驗收

1. 在約 1220px 寬桌面視窗比較行情表與策略表：標題與每列邊界必須完整對齊，底線橫跨全列。
2. 關閉所有詳情時檢查前五列：自選與判定維持窄欄，策略路徑與市場承接取得較寬空間，日利與資金保持單行靠右。
3. 所有儲存格必須維持原生 `table-cell`；多行排版只存在儲存格內層，不能再以 Grid 取代 `<td>`。
4. 手機寬度以策略表容器水平捲動，頁面本體不能產生水平溢出；左側自選與策略欄保持可辨識。
5. 展開一列「理由」或「步驟」時，只增加該列高度，不得改變其餘列或欄寬。
